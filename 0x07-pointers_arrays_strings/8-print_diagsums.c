#include "holberton.h"
#include <stdio.h>
/**
* print_diagsums - function
* @a : pointer
* @size : size
*/

void print_diagsums(int *a, int size)
{
	int i, j;
	char r;

	for (i = 0 ; i < size ; i++)
	{
		for (j = 0; j < size ; j++)
		{
			r = a[i][j];
		}
	}
}
