#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
* *_realloc - function that reallocates a memory block using malloc and free
* @ptr :
* @old_size : 
* @new_size :
* Return:
*/

void *_realloc(void *ptr, unsigned int old_size, unsigned int new_size)
{
	char *d;
	if (new_size == )

	d = malloc(sizeof(char) * new_size)
	if (d == NULL)
		returnn (NULL);	
}
